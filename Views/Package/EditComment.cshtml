@model Remood_Place.Models.PackageComment

@{
    ViewData["Title"] = "EditComment";
}

<h1>EditComment</h1>

<h4>PackageComment</h4>
<hr />

@using (Html.BeginForm("EditComment", "Package", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="modal-body" style="max-height: 400px; overflow-y: auto;">
        <div class="mb-3">
            <input type="number" hidden class="form-control" name="Packageid" value="@ViewBag.id">
        </div>
        <div class="mb-3">
            <label for="comment" class="form-label">Write Comment</label>
            <input type="text" class="form-control" id="comment" name="newComment">
        </div>
    </div>

    <div class="modal-footer" style="justify-content: space-between;">
        <button type="submit" class="btn btn-primary submit m-0 p-0 dep rounded-pill" style="width: 100px; height: 50px;">
            <label class="mb-0 mx-auto ms-2">Add</label>
            <i class="fa-solid fa-check"></i>
        </button>
        <button type="button" class="btn btn-danger bg-danger p-0 rounded-pill" style="width: 100px; height: 50px;" data-bs-toggle="modal" data-bs-target="#exampleModal">
            <label class="mb-0 mx-auto ms-2">Cansel</label>
            <i class="fa-solid fa-xmark "></i>
        </button>
    </div>
}
@* <div class="row">
    <div class="col-md-4">
        <form asp-action="EditComment">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Commentt" class="control-label"></label>
                <input asp-for="Commentt" class="form-control" />
                <span asp-validation-for="Commentt" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PackageId" class="control-label"></label>
                <select asp-for="PackageId" class="form-control" asp-items="ViewBag.PackageId"></select>
                <span asp-validation-for="PackageId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UserId" class="control-label"></label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.UserId"></select>
                <span asp-validation-for="UserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div> *@

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
